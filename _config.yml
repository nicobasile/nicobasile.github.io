#
# This file contains configuration flags to customize your site
#

# Name of your site (displayed in the header)
name: Nico Basile

# Used by jekyll-seo-tag and jekyll-feed, which read site.title rather than site.name
title: Nico Basile

lang: en
locale: en_US

# Name of the author
author: Nicolas Basile

# Used by jekyll-seo-tag and jekyll-feed for search snippets and social cards
description: Personal research on intelligent systems, embodied AI, robot policy training, and accelerator infrastructure.

# Short line displayed under the name in the header
tagline: A collection of experiments

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: /images/robotic_gripper_neural_net_no_bg.png
#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble:
  email: nico.basile24@gmail.com
  facebook:
  flickr:
  github: nicobasile
  instagram:
  linkedin: nicobasile
  pinterest:
  twitter: nicobasile_
  stackoverflow: 
  youtube: # channel/<your_long_string> or user/<user-name>
  googleplus: # anything in your profile username that comes after plus.google.com/
  playconsole:
  mastodon: # <your-mastodon-instance>/@<your-user-name> eg. fosstodon.org/@username

# Google Analytics 4 measurement ID, e.g. G-GABC1DEFG
#google_analytics_ga4:

# Your website URL (e.g. http://amitmerchant1990.github.io or http://www.amitmerchant.com)
# Used for Sitemap.xml and your RSS feed
url: https://nicobasile.github.io
enforce_ssl:

twitter:
  username: nicobasile_
  card: summary_large_image

social:
  name: Nicolas Basile
  links:
    - https://github.com/nicobasile
    - https://www.linkedin.com/in/nicobasile
    - https://twitter.com/nicobasile_

# If you're hosting your site at a Project repository on GitHub pages
# (http://yourusername.github.io/repository-name)
# and NOT your User repository (http://yourusername.github.io)
# then add in the baseurl here, like this: "/repository-name"
baseurl: ""

#
# !! You don't need to change any of the configuration flags below !!
#

permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass:
  style: expanded # You might prefer to minify using :compressed

# Use the following plug-ins
plugins:
  - jekyll-sitemap # Create a sitemap using the official Jekyll sitemap gem
  - jekyll-feed # Create an Atom feed using the official Jekyll feed gem
  - jekyll-seo-tag
  - jekyll-paginate

include: ['_pages']

# Gives every page an `image` so jekyll-seo-tag emits og:image, twitter:image,
# and a summary_large_image card. Override per post with `image:` in front matter.
defaults:
  - scope:
      path: ""
    values:
      image: /images/robotic_gripper_neural_net_no_bg.png

paginate: 6
paginate_path: /page:num/

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME

  - scripts
